export enum Subjects {
    TicketCreated = 'ticket:created',
    OrderUpdated = 'order:updated',
}

// Example - How to use:
// function testEnum(sub: Subjects) {
//     console.log(sub);
// }

// testEnum(Subjects.TicketCreated);
